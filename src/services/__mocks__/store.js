const store = {
  id: 1,
  name: 'testStore',
  fee: 10,
};

const createStore = (store) => {
  return store;
};

const updateStore = (store) => {
  return store;
};

const getStore = () => {
  return store;
};

module.exports = {
  createStore,
  updateStore,
  getStore,
};
